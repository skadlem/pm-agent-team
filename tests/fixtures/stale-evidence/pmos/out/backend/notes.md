# Backend notes

Implemented T-001. graphify queries run before editing: `query_graph "reset token"`,
`get_neighbors src_auth_reset`. Token redemption marks the row redeemed in the same
transaction as the check.
